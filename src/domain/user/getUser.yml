유저 회원 가입을 진행한다.
---
parameters:
  - name: user-create
    in: path
    type: int
    schema:
      $ref: "#/definitions/UserCreate"
definitions:
  UserCreate:
    type: object
    properties:
      email:
        type: string
      password:
        type: string
responses:
  200:
    description: Get all user data
